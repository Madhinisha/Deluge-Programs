// 1. Write a Deluge Script to check whether a character is uppercase orlowercase alphabet.
char = "w";
if(char.matches("[A-Z]"))
{
	info "The Given character is Uppercase";
}
else 
{
	info "The Given character is Lowercase";
}
=================================================================================================================================
// Write a script to input a month number and print the number of days in that month.
month = 2;
year = 2004;
if(month.matches(2) && (year%4==0 && year%100 !=0) || year%400 == 0)
{
	info "The Month " + month + " has 29 days";
}
else  if(month.matches(1)|| month.matches(3)||month.matches(5)||month.matches(7)||month.matches(8)|| month.matches(12)||month.matches(10))
{
 info "The Month " + month + " has 31 days";
}
else  if(month.matches(4)|| month.matches(6)||month.matches(9)||month.matches(11))
{
 info "The Month " + month + " has 30 days";
}
else  if(month.matches(2))
{
 info "The Month " + month + " has 28 days";
}
=================================================================================================================================
// 3. Write a deluge script to count the total number of notes in given amount.

amount = 34567;
Five_Hundred = (amount/500).floor();
balance = amount%500;
if(Five_Hundred >= 1)
{
	info "Total number of 500rs note is : " +Five_Hundred;
}
Two_hundred = (balance/200).floor();
balance = balance%200;
if(Two_hundred>= 1)
{
	info "Total number of 200rs note is : " +Two_hundred;
}
Hundred = (balance/100).floor();
balance = balance%100;
if(Hundred >=1)
{
	info "Total number of 100rs note is : " +Hundred;
}
Fifety = (balance/50).floor();
balance = balance%50;
if(Fifety >=1)
{
	info "Total number of 50rs note is : " +Fifety;
}
twenty = (balance/20).floor();
balance = balance%20;
if(twenty >=1)
{
	info "Total number of 20rs note is : " +twenty;
}
Ten = (balance/10).floor();
balance=balance%10;
if(Ten >=1)
{
	info "Total number of 10rs note is : " +Ten;
}
info "Remaining Balance : "+balance;
=================================================================================================================================
// 4. Write a script to input marks of five subjects: Physics, Chemistry, Biology,
// Mathematics, and Computer. Calculate percentage and grade according to the
// following:
//             Percentage >= 90% : Grade A
//             Percentage >= 80% : Grade B
//             Percentage >= 70% : Grade C
//             Percentage >= 60% : Grade D
//             Percentage >= 40% : Grade E
//             Percentage < 40% : Grade 

Marks = {"Physics" : 90, "Chemistry":80, "Biology" : 56,"Mathematics":78,"Computer":98};
total = 0;
for each mark in Marks
{
    total = total + mark;
}
percentage = total / 5;
info "Total Marks: " + total;
info "Percentage: " + percentage;
if(percentage >= 90)
{
    info "Grade A";
}
else if(percentage >= 80)
{
    info "Grade B";
}
else if(percentage >= 70)
{
    info "Grade C";
}
else if(percentage >= 60)
{
    info "Grade D";
}
else if(percentage >= 40)
{
    info "Grade E";
}
else
{
    info "Grade F";
}
=================================================================================================================================
// 5. Write a deluge script to input the basic salary of an employee and calculate its
// Gross salary according to the following:
//             Basic Salary <= 10000 : HRA = 20%, DA = 80%
//             Basic Salary <= 20000 : HRA = 25%, DA = 90%
//             Basic Salary > 20000 : HRA = 30%, DA = 95%

Basic_Salary = 15000;
if(Basic_Salary <= 10000)
{
	HRA = Basic_Salary * 0.20;
	DA = Basic_Salary *0.80;
}
if(Basic_Salary <= 20000 )
{
	HRA = Basic_Salary *0.25;
	DA = Basic_Salary * 0.90;
}
if(Basic_Salary>20000)
{
	HRA = Basic_Salary *0.30;
	DA = Basic_Salary *0.95;
}
Gross_Salary = Basic_Salary + HRA + DA;
info "Gross Salary : " + Gross_Salary;
=================================================================================================================================
// 6. Write a deluge script to input electricity unit charges and calculate the total
// electricity bill according to the given condition:
// For first 50 units Rs. 0.50/unit
//             For next 100 units Rs. 0.75/unit
//             For next 100 units Rs.1.20/unit
//             For unit above 250 Rs.1.50/unit
//             An additional surcharge of 20% is added to the bill
 
units = 500;
bill = 0;

if(units <= 50)
{
    bill = units * 0.50;
}
else if(units <= 150)
{
    bill = (50 * 0.50) + ((units - 50) * 0.75);
}
else if(units <= 250)
{
    bill = (50 * 0.50)  + (100 * 0.75) + ((units - 150) * 1.20);
}
else
{
    bill = (50 * 0.50) + (100 * 0.75) + (100 * 1.20)  + ((units - 250) * 1.50);
}

// Add 20% surcharge
surcharge = bill * 0.20;
total_bill = bill + surcharge;

info "Electricity Bill before surcharge: " + bill;
info "Surcharge : " + surcharge;
info "Total Electricity Bill: " + total_bill;
=================================================================================================================================
// 7. Write a deluge script for a shopkeeper who buys a product for 15 rs and he sells
// the product for Rs.25 . Check whether the shopkeeper had profit or loss for these
// sales.

Bought = 70;
sold = 20;
Value = (Bought - sold).abs();
if(Value > Bought)
{
	info "The shopkepper had a profit amount of : " + Value;
}
if(Value < Bought)
{
	info "The shopkepper had a loss amount of : " + Value;
}
=================================================================================================================================
// 8. Write a deluge script to input any string and check whether the string is
// Palindrome or Not.
 
word = "Madhinisha";
str = word.toLowerCase();
if(str.reverse()== str)
{
	info "palindrome";
}
else 
{
	info "Not a palindrome";
}
=================================================================================================================================
// 9. Given two numbers, print "Equals" if the square root of the first number is equal to
// the square of the second number and vice versa.
num1 = 2;
num2 = 4;
if(num1.sqrt()==num2.sqrt())
{
	info "Equal";
}
else 
{
	info "Not Equal";
}
=================================================================================================================================
// 10.Print the difference between its flooring value and ceiling value for the given two
// numbers and print the absolute value for it.
 
a = 6.55;
b=9.57;
Ceil1 = a.ceil();
Floor1 = a.floor();
Ceil2 = b.ceil();
Floor2 = b.floor();
diff1 = (Floor1 - Ceil1).abs();
diff2 = (Floor2 - Ceil2).abs();
info "Floor and Ceil of First number is : " + Floor1 + " & " +Ceil1 +" (abs value : "+diff1 +")";
info "Floor and Ceil of Second number is : " + Floor2 + " & " +Ceil2 +" (abs value : " +diff2+")";

=================================================================================================================================
// 11.Write a program to perform addition, subtraction, multiplication, and division of
// any two numbers using List.
list1 = {2,3};
add = list1.get(0) + list1.get(1);
sub = list1.get(0) - list1.get(1);
mul = list1.get(0) * list1.get(1);
div = list1.get(0) / list1.get(1);
info "{ Add : "+add+" , Sub : "+sub+" , Mul : "+mul+" , Div : "+div+" }";
=================================================================================================================================
// 12.If the given input is even, change it to the next odd number, and if it is odd,
// change it to the square of the number. Print the changed number in both the
// cases.

num = 92;
if(num %2==0)
{
	odd = num+1;
	info "The Given number is even so the next odd number is : " + odd;
}
else if(num%2==1)
{
	square = num * num;
	info "The Given number is odd so the Square root of the number is : "+ square;
}
=================================================================================================================================
// 13.Print Simple Interest and Compound Interest after initializing all the necessary
// inputs.

P = 10000;
R = 5;
T = 2;
SI = (P * R * T) / 100;
rate = R / 100.0; 
CI = P *(1 + rate).power(T)-P;
info "Simple Interest: " + SI;
info "Compound Interest: " + CI;
=================================================================================================================================
